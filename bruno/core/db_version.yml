info:
  name: db_version
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}:{{port}}/api/db_version/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
