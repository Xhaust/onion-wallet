info:
  name: me
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}:{{port}}/api/users/me/"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("200 OK", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
