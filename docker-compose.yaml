services:
  frontend:
    build: ./frontend
    ports: 
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
  backend:
    build: ./backend
    env_file:
      - .env
    ports: 
      - "8000:8000"
    volumes:
      - ./backend:/app